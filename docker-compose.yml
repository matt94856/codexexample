version: '3.9'
services:
  backend:
    build: ./backend
    env_file: .env
    ports:
      - '4000:4000'
  frontend:
    build: ./frontend
    environment:
      - VITE_API_BASE_URL=http://backend:4000/api
    ports:
      - '8080:80'
    depends_on:
      - backend
